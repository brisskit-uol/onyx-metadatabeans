*===========================================================================*
* Project that generates xmlbeans used for manipulating onyx metadata
*===========================================================================*

The schema xsd's have been generated by BRISSKit.
They have not been copied from elsewhere!

*==============*
*  IMPORTANT   *
*==============*
To build this project, always (always!) run a clean beforehand...

mvn clean install

or

mvn clean deploy

The clean cycle will remove the annoying staleFlag from the target directory.
Otherwise, the results will not be what you expected  :-)